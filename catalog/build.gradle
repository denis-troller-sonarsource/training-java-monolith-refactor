plugins {
    id 'java-library'
}

// Billing-category (catalog) bounded context. Code is carved in from :app in Phase 3.
dependencies {
    api project(':common')

    compileOnly('jakarta.enterprise:jakarta.enterprise.cdi-api:4.1.0')
}
